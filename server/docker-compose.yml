services:
  postgres:
    image: postgres:16-alpine
    container_name: implementer-db
    environment:
      POSTGRES_USER: implementer
      POSTGRES_PASSWORD: implementer
      POSTGRES_DB: implementer_io
    ports:
      - "5434:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
